import sys
print(sys.maxsize)      #shows how much size internally the variable can take
print(sys.path)        #to check the prog path and as well as the built-in path
print(sys.version)     #shows which is the python version
print(sys.modules)
print(len(sys.modules))
print((sys.platform))          #to check which platform the sys is using
print(sys.executable)   #to check the current prog path
print(sys.exit())
print(sys.path)